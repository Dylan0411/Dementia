using UnityEngine;

public class puzzleConcept1_Destination : MonoBehaviour
{
    public int idNumber = 0;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

    // This method is called when another collider enters the trigger collider attached to this object
    //void OnTriggerEnter(Collider trigger)
    //{
     //   // Get the puzzleConcept1_Item component from the collided object
      //  puzzleConcept1_Item item = trigger.GetComponent<puzzleConcept1_Item>();
//
 //       // Check if the item is not null and if the idNumber matches
  //      if (item.idNumber == idNumber)
   //     {
    //        Debug.Log("it works");
     //   }
    //}
}
